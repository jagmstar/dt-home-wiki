name: CI
on:
  push:
    branches: ["main", "master"]
  pull_request:
jobs:
  basic-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Basic validation
        run: |
          echo "Running basic CI for ${{ github.repository }}"
          echo "Lint placeholder passed"
